<div class="card" style="margin-top:16px;"><div class="card-header"><div><div class="card-title">Data Penunjang</div></div></div><div class="table-wrapper"><table class="table table-striped"><thead><tr><th>Nama Kegiatan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($penunjangs as $item)<tr><td>{{ $item->nama_kegiatan }}</td><td><span class="badge {{ $item->status === 'approved' ? 'badge-success' : ($item->status === 'rejected' ? 'badge-danger' : 'badge-warning') }}">{{ ucfirst($item->status) }}</span></td><td>@if($item->status === 'pending')<div class="table-actions"><a href="{{ route('kinerja-saya.penunjang.edit', $item) }}" class="btn btn-secondary">Edit</a><form data-confirm="true" data-confirm-type="danger" data-confirm-title="Hapus Data?" data-confirm-body="Data penunjang akan dihapus permanen." method="POST" action="{{ route('kinerja-saya.penunjang.destroy', $item) }}">@csrf @method('DELETE')<button class="btn btn-danger">Hapus</button></form></div>@else - @endif</td></tr>@empty<tr><td colspan="3">Belum ada data.</td></tr>@endforelse</tbody></table></div></div>
